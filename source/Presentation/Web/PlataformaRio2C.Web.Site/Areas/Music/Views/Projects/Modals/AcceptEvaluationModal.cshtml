@model PlataformaRio2C.Application.CQRS.Commands.AcceptMusicProjectEvaluation
<div class="modal fade" id="AcceptEvaluationModal" role="dialog" aria-labelledby="AcceptEvaluationModal" aria-hidden="true" data-backdrop="static" data-keyboard="false">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">
                    @Labels.AcceptProject @Labels.Project
                </h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                @using (Html.BeginForm("Accept", "Projects", new { Area = "Music" }, FormMethod.Post, new { id = "AcceptEvaluationForm" }))
                {
                    @Html.HiddenFor(m => m.ProjectUid)
                    <div id="form-container">
                        @Html.Partial("Modals/AcceptEvaluationForm", Model)
                    </div>
                }
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary mr-auto" data-dismiss="modal">@Labels.Cancel</button>
                <button type="button" class="btn btn-success" onclick="$('#AcceptEvaluationForm').submit();">
                    <i class="fa fa-thumbs-up"></i> @Labels.AcceptProject
                </button>
            </div>
        </div>
    </div>
</div>