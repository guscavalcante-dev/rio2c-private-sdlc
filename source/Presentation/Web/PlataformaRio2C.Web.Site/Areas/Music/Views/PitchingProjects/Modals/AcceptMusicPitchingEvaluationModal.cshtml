@model PlataformaRio2C.Application.CQRS.Commands.AcceptMusicPitchingEvaluation
<div class="modal fade" id="AcceptMusicPitchingEvaluationModal" role="dialog" aria-labelledby="AcceptMusicPitchingEvaluationModal" aria-hidden="true" data-backdrop="static" data-keyboard="false">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">
                    @Labels.AcceptProject @Labels.Project
                </h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                @using (Html.BeginForm("Accept", "PitchingProjects", new { Area = "Music" }, FormMethod.Post, new { id = "AcceptMusicPitchingEvaluationForm" }))
                {
                    @Html.HiddenFor(m => m.MusicBandUid)
                    <div id="form-container">
                        <div class="form-group col-12">
                            @Html.Raw(string.Format(Texts.DoYouReallyWishAccept, Model.MusicProjectDto?.AttendeeMusicBandDto?.MusicBand?.Name ?? "-"))
                        </div>
                    </div>
                }
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary mr-auto" data-dismiss="modal">@Labels.Cancel</button>
                <button type="button" class="btn btn-success" onclick="MusicProjectsEvaluationWidget.submitAcceptEvaluation();">
                    <i class="fa fa-thumbs-up"></i> @Labels.AcceptProject
                </button>
            </div>
        </div>
    </div>
</div>