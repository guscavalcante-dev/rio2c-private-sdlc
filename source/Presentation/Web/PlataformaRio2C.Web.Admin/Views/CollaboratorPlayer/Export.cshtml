@model IEnumerable<PlataformaRio2C.Application.ViewModels.CollaboratorPlayerItemListAppViewModel>

@{
    ViewBag.Title = Labels.Executives;
}

<div class="">
    <section class="panel .panel-lg">
        <header class="panel-heading clearfix">
            <h2 class=" pull-left">@ViewBag.Title</h2>

            <a href="@Url.Action("Create")" class="btn btn-sm btn-success pull-right">
                @Labels.RegisterNewExecutive
            </a>
        </header>

        <div class="profile-info">

            <div class="count-list-results">
                &nbsp;
            </div>

            <div class="wrap-table table-responsive">
                <table class="table table-striped table-advance table-hover">
                    <thead>
                        <tr>
                            <th>
                                @Html.DisplayNameFor(model => model.Name)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Email)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.HoldingsName)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.PlayersName)
                            </th>
                            <th>
                                @*@Html.DisplayNameFor(model => model.Job)*@
                            </th>
                            <th>
                                @*@Html.DisplayNameFor(model => model.MiniBio)*@
                            </th>

                            <th>
                                @Html.DisplayNameFor(model => model.CreationDate)
                            </th>
                            <th class="editExcludeTd"></th>
                        </tr>
                    </thead>
                    <tbody>
                        @*@foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Name)
                                    </td>

                                    <td>
                                        @Html.DisplayFor(modelItem => item.Email)
                                    </td>

                                    <td>
                                        @Html.DisplayFor(modelItem => item.HoldingsName)
                                    </td>

                                    <td>
                                        @Html.DisplayFor(modelItem => item.PlayersName)
                                    </td>

                                    <td>
                                        @Html.DisplayFor(modelItem => item.CreationDate)
                                    </td>
                                    <td>
                                        @Html.ActionLink("Editar", "Edit", new { uid = item.Uid }) |
                                        <span>
                                            <a data-toggle="modal" href="#modalDelete" data-urltarget="@Url.Action("Delete", new { uid = item.Uid})"> Excluir </a>
                                        </span>
                                    </td>
                                </tr>
                            }*@
                    </tbody>
                </table>
            </div>
        </div>


    </section>
</div>

@Html.Partial("_ModalDelete")