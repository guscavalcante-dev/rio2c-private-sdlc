@model PlataformaRio2C.Application.CQRS.Commands.UpdateEditionDatesInformation
<div class="modal fade" id="UpdateDatesInformationModal" role="dialog" aria-labelledby="UpdateDatesInformationModal" aria-hidden="true" data-backdrop="static" data-keyboard="false">
    <div class="modal-dialog modal-dialog-centered modal-lg" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">
                    @(Labels.Update + " " + Labels.Informations)
                </h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                @using (Html.BeginForm("UpdateDatesInformation", "Editions", new { Area = "" }, FormMethod.Post, new { id = "UpdateDatesInformationForm" }))
                {
                    @Html.HiddenFor(m => m.EditionUid)
                    <div id="form-container">
                        @Html.Partial("Modals/_UpdateDatesInformationForm", Model)
                    </div>
                }
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary mr-auto" data-dismiss="modal">@Labels.Cancel</button>
                <button type="button" class="btn btn-primary" onclick="$('#UpdateDatesInformationForm').submit();">@Labels.Save</button>
            </div>
        </div>
    </div>
</div>