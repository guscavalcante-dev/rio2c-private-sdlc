@model string
<h3 class="kt-heading kt-heading--sm mb-0 mt-0">Youtube</h3>
<div class="row mt-3">
    <div class="col-12">
        @if (!string.IsNullOrEmpty(Model))
        {
            <a href="@Model.GetUrlWithProtocol()" class="btn btn-icon btn-circle btn-label-youtube" target="_blank">
                <i class="socicon-youtube"></i>
            </a>
            <a href="@Model.GetUrlWithProtocol()" target="_blank">@Model</a>
        }
        else
        {
            @:-
        }
    </div>
</div>