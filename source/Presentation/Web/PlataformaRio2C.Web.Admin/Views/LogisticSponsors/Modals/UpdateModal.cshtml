@model PlataformaRio2C.Application.CQRS.Commands.UpdateLogisticSponsors
<div class="modal fade" id="UpdateLogisticSponsorModal" role="dialog" aria-labelledby="UpdateLogisticSponsorModal" aria-hidden="true" data-backdrop="static" data-keyboard="false">
    <div class="modal-dialog modal-dialog-centered modal-lg" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">
                    @if (!Model.IsAddingToCurrentEdition)
                    {
                        @(Labels.Update + " " + Labels.Sponsor)
                    }
                    else
                    {
                        @(Labels.NewM + " " + Labels.Sponsor + " " + string.Format(Labels.InEditionYear, ViewBag.EditionDto?.UrlCode))
                    }
                </h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                @using (Html.BeginForm("Update", "LogisticSponsors", new { Area = "" }, FormMethod.Post, new { enctype = "multipart/form-data", id = "UpdateLogisticSponsorForm" }))
                {
                    @Html.HiddenFor(m => m.LogisticSponsorUid)
                    @Html.HiddenFor(m => m.IsAddingToCurrentEdition)
                    <div id="form-container">
                        @Html.Partial("Modals/_CreateForm", Model)
                    </div>
                }
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary mr-auto" data-dismiss="modal">@Labels.Cancel</button>
                <button type="button" class="btn btn-primary" onclick="$('#UpdateLogisticSponsorForm').submit();">@Labels.Save</button>
            </div>
        </div>
    </div>
</div>