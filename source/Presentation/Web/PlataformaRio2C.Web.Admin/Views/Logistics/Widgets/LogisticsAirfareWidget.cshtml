@using PlataformaRio2c.Infra.Data.FileRepository.Helpers
@using PlataformaRio2C.Domain.Dtos
@using PlataformaRio2C.Domain.Statics
@model ConferenceDto
<div class="kt-portlet__head">
    <div class="kt-portlet__head-label">
        <h3 class="kt-portlet__head-title">
            @Labels.Airfare
        </h3>
    </div>
    <div class="kt-portlet__head-toolbar">
        <div class="kt-portlet__head-actions">
            <button type="button" class="btn btn-clean btn-sm btn-icon btn-icon-md btn-circle" onclick="LogisticAirfareCreate.showModal();">
                <i class="flaticon2-add-1"></i>
            </button>
        </div>
    </div>
</div>
<div class="kt-portlet__body pt-0">
    <div class="kt-widget">
        <div class="kt-widget__bottom">
            <div class="row">
                <div class="col-md-12">
                    <div class="kt-widget4">
                        @if (Model != null)
                        {
                            <div class="col-12">
                                <table class="table table-sm table-head-bg-brand table-body-scroll">
                                    <thead class="thead-inverse">
                                        <tr>
                                            <th>@Labels.Name</th>
                                            <th class="text-center">@Labels.Year</th>
                                        </tr>
                                    </thead>
                                    <tbody class="rio2c-scroll" style="height: 180px;">
                                        @*foreach (var releasedMusicProject in Model)
                                        {
                                            <tr>
                                                @*<td>
                                                        <a href="@Url.Action("Details", "Conferences", new { Area = "", id = musicBandTeamMember.Conference.Uid })" target="_blank">
                                                            @(musicBandTeamMember.GetConferenceTitleDtoByLanguageCode(ViewBag.UserInterfaceLanguage)?.ConferenceTitle?.Value)
                                                        </a>
                                                    </td>
                                                <td>@(releasedMusicProject.Name)</td>
                                                <td class="text-center">@(releasedMusicProject.Year)</td>
                                            </tr>
                                        }*@
                                    </tbody>
                                </table>
                            </div>
                        }
                        else
                        {
                            <div class="row mt-3">
                                <div class="col-12 text-center">
                                    <span class="text-center">@string.Format(Messages.EntityNotAction, Labels.ProjectsReleased, Labels.FoundMP)</span>
                                </div>
                            </div>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>