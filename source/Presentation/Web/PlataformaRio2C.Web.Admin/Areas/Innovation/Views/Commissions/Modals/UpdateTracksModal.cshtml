@model PlataformaRio2C.Application.CQRS.Commands.UpdateInnovationCollaboratorTracks
<div class="modal fade" id="UpdateTracksModal" role="dialog" aria-labelledby="UpdateTracksModal" aria-hidden="true" data-backdrop="static" data-keyboard="false">
    <div class="modal-dialog modal-dialog-centered modal-lg" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">
                    @(Labels.Update + " " + Labels.Tracks)
                </h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                @using (Html.BeginForm("UpdateTracks", "Commissions", new { Area = "Innovation" }, FormMethod.Post, new { enctype = "multipart/form-data", id = "UpdateTracksForm" }))
                {
                    @Html.HiddenFor(m => m.CollaboratorUid)
                    <div id="form-container">
                        @Html.Partial("Modals/UpdateTracksForm", Model)
                    </div>
                    @*<div class="row">
                        <div class="col-sm-12 text-right">
                            @Labels.LastUpdateOn <b>@Model.UpdateDate.ToBrazilTimeZone().ToShortDateString() @Model.UpdateDate.ToBrazilTimeZone().ToLongTimeString()</b> @Labels.By <b>@Model.UpdaterBaseDto.Name</b>.
                        </div>
                    </div>*@
                }
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary mr-auto" data-dismiss="modal">@Labels.Cancel</button>
                <button type="button" class="btn btn-primary" onclick="$('#UpdateTracksForm').submit();">@Labels.Save</button>
            </div>
        </div>
    </div>
</div>