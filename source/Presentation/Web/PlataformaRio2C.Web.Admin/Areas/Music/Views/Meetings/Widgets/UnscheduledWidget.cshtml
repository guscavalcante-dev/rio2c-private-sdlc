@using PlataformaRio2C.Domain.Dtos
@model List<MusicBusinessRoundProjectBuyerEvaluationDto>
<div class="kt-portlet kt-portlet--mobile">
    <div class="kt-portlet__head kt-portlet__head--lg">
        <div class="kt-portlet__head-label">
            <span class="kt-portlet__head-icon">
                <i class="kt-font-brand flaticon2-line-chart"></i>
            </span>
            <h3 class="kt-portlet__head-title">
                @Labels.UnscheduledNegotiations
            </h3>
        </div>
    </div>
    <div class="kt-portlet__body">
        @if (Model?.Any() == true)
        {
            <!--begin: Data -->
            <table class="table table-striped- table-bordered table-hover table-checkable" id="conferences-list-table">
                <thead>
                    <tr>
                        <th>@Labels.Player</th>
                        <th>@Labels.Participant</th>
                        <th class="text-center">@Labels.Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var MusicBusinessRoundProjectBuyerEvaluation in Model)
                    {
                        <tr>
                            <td>@(MusicBusinessRoundProjectBuyerEvaluation.MusicBusinessRoundProjectBuyerEvaluation?.BuyerAttendeeOrganization?.Organization?.TradeName)</td>
                            <td>@(MusicBusinessRoundProjectBuyerEvaluation.MusicBusinessRoundProjectBuyerEvaluation?.MusicBusinessRoundProject?.SellerAttendeeCollaborator?.Collaborator?.GetStageNameOrBadgeOrFullName())</td>
                            <td class="text-center">
                                <span class="dropdown">
                                    <a href="#" class="btn btn-sm btn-clean btn-icon btn-icon-md" data-toggle="dropdown" aria-expanded="true">
                                        <i class="la la-ellipsis-h"></i>
                                    </a>
                                    <div class="dropdown-menu dropdown-menu-right">
                                        <button class="dropdown-item" onclick="MusicMeetingsManualSchedule.showModal('@(MusicBusinessRoundProjectBuyerEvaluation.MusicBusinessRoundProjectBuyerEvaluation.Uid)');">
                                            <i class="la la-calendar"></i>@Labels.ScheduleManually
                                        </button>
                                        @*<button class="dropdown-item" onclick="MusicMeetingsScheduledDelete.showModal('@(negotiation?.Uid)');">
                        <i class="la la-remove"></i>@Labels.Remove
                    </button>*@
                                    </div>
                                </span>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
            <!--end: Data -->
        }
        else
        {
            <div class="col-12 text-center mt-30">@string.Format(Messages.EntityNotAction, Labels.UnscheduledNegotiations, Labels.FoundFP)</div>
        }
    </div>
</div>